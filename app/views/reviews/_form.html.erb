<%= form_for([@hotel, @review]) do |f| %>

    <%= f.label :date_visited %>
    <%= f.date_select :date_visited %>

    <%= f.label :content %>
    <%= f.text_field :content %>

    <%= f.submit %>

<% end %>